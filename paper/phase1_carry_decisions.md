# Phase 1 Mechanical Carry — Locked Decisions (Q-mig-1..5 rulings)

> **Status (2026-05-16):** Leader ruled on 5 migration questions and
> the Round-19+2 MLCAD-abandon update. Captured here as the durable
> reference for when outline_v4 dual review closes and Phase 1
> mechanical carry executes. **Do not re-litigate any of these.**
> If a contradiction surfaces during Phase 1 execution, flag to
> leader before proceeding.

## Q-mig-1: Plan D revert — **APPROVED**

**Action at Phase 1:** When carrying v3 `sec/results.tex` Table 5 to
`v4/sec/results.tex` §6.1, **strip** the compaction commands
inserted in Round-17:

```latex
\renewcommand{\arraystretch}{0.88}     % REMOVE
\setlength{\tabcolsep}{3pt}            % REMOVE
```

Revert to acmart `acmsmall` defaults. Rationale: long-form journal
layout has page room; the compaction was an MLCAD 6pp typesetting
hack with no continuing benefit.

**Side-check:** verify no other `\arraystretch` / `\tabcolsep`
overrides exist in v3 sec/*.tex via grep before bulk carry.

## Q-mig-2: §5.1 / §5.5 un-trim — **RESTORE from sec5_results.md**

**Action at Phase 1:** Treat `paper/sec5_results.md` as the canonical
pre-trim source for §5.1 TL;DR + §5.5 operating-point text. The v3
`sec/results.tex` versions (trimmed 12→9 and 10→8 lines in
Round-17/18 for MLCAD page-budget) are **superseded by the md
source** for v4.

Concrete restore steps:
1. Open `paper/sec5_results.md`, locate §5.1 TL;DR + §5.5
   operating-points sections.
2. Translate from md → tex (the only mechanical work — table syntax,
   `\cite`, `\S\ref`, italic / bold). No prose changes.
3. Drop the Plan-C single-line ± notation rollback question — Plan
   C cells stay as-is (cell-level decision was data, not page-budget).
4. After Phase 1 carry completes, **archive** `sec5_results.md` (mark
   with a `> ARCHIVED — superseded by v4/sec/results.tex 2026-MM-DD`
   header at top, do not delete).

**Source-of-truth note:** This is the **single exception** to the
Q-mig-3 ruling that md files are archived; sec5_results.md is the
last-read md after this step.

## Q-mig-3: Md → tex source-of-truth switch — **APPROVED**

**Action at Phase 1 close:** Once v4 has all sections live in tex,
add a one-line ARCHIVED banner to the top of each `paper/sec*.md`
file:

```
> ARCHIVED 2026-MM-DD — v4 source-of-truth lives in
> `paper/v4/sec/<section>.tex`. Do not edit this file.
```

Files to mark: `sec1_intro.md`, `sec1_intro_v3.md`,
`sec2_related_v2.md`, `sec2_related_v3.md`, `sec3_platform.md`,
`sec4_methodology.md`, `sec4_safe_bridge_threat_model.md`,
`sec5_results.md` (after Q-mig-2 restore), `sec6_failures.md`,
`sec7_discussion.md`, `sec8_conclusion.md`.

**Implementation discipline going forward:** all v4 edits live in
`paper/v4/sec/*.tex`; no md→tex regeneration step exists in v4.

## Q-mig-4: `analogcoderpro` resolution — **OPTION (c) KEEP + resolve (final 2026-05-16)**

**History (3 rounds):**
1. Initial ruling = *"REMOVE from refs.bib"* on basis "never cited".
2. paper_writer caught premise-error: `analogcoderpro` IS cited at
   v3 `sec/related.tex:9`. Reported, paused execution.
3. Leader re-ruled with corrected premise → **option (c) KEEP +
   resolve via WebSearch+arXiv** rather than remove. WebSearch
   resolved: Lai et al., AnalogCoder follow-up,
   arXiv:2508.02518, submitted 2025-08-04. Entry restored.

**Executed 2026-05-16 (final state):**
- `paper/refs.bib`: full `@article{analogcoderpro,...}` entry restored
  with 8-author Lai et al. list, canonical title, arXiv ID, URL, code
  repo. Preceding comment block records the 3-round history.
- `paper/refs_TBD_audit.md`: R2 closed with new downstream risk
  cross-link to Phase-2 §2.2 reframe.
- v3 build no longer broken (entry resolves the `\cite{analogcoderpro}`
  at `sec/related.tex:9` again).

**Phase-1 mechanical carry action (REVERSED from prior log):**
Previously this log said *"drop the AnalogCoder-Pro mention entirely,
verb-agreement fix `frames`"*. **That instruction is now WRONG and
must not be applied.** Correct Phase-1 action:
- Carry the v3 Designer-cluster sentence **verbatim** including the
  `\cite{analogcoderpro}` reference. No prose change at Phase-1
  scope. The reframe is Phase-2 work.

**Phase-2 expansion action (NEW) — §2.2 reframe vs AnalogCoder-Pro:**

AnalogCoder-Pro's abstract reveals three modules: (i) rejection-
sampling fine-tuning, (ii) MLLM netlist generation, (iii) LLM-
assisted circuit sizing with Bayesian optimization. **Module (iii)
overlaps our sizing work**, so the v3 framing *"complementary, code
generation from spec"* is no longer accurate.

Phase-2 §2.2 prose to replace the v3 hedge (leader-drafted, paper
writer to finalize at Phase-2 time):

> "AnalogCoder~\cite{analogcoder} treats analog design as
> training-free code generation from spec. AnalogCoder-Pro~\cite{analogcoderpro}
> extends this with multimodal diagnosis-and-repair feedback and a
> Bayesian-optimization sizing module evaluated on open BSIM-45nm
> benchmarks. Our work differs along two axes: (i) we operate
> inside a closed foundry PDK under NDA constraints rather than on
> idealized open-source models, and (ii) we benchmark eleven
> frontier checkpoints across the cost-quality Pareto rather than
> fielding a single LLM agent."

Two-axis differentiation: (a) NDA-PDK vs open BSIM, (b) eleven-
checkpoint Pareto vs single-agent fielding.

**Cross-reference for Phase-2 reviewer**: also re-examine the
Differentiation Matrix (v3 `sec/related.tex` Table 1, ~L47): the
`analogcoderpro` row currently does not exist there; Phase-2 should
**add a row** with appropriate ✓/✗/partial entries across the 6
axes, especially #China-frontier (0), cost-quality Pareto (✗), and
NDA-safe substrate (✗) — the three columns where Pro is weakest
relative to us. The new row will reinforce C3 + C4 + C2 simultaneously.

## Q-mig-5: Pricing snapshot — **PIN 2026-05-01, framed as "frozen for reproducibility"**

**Action at Phase 1:** When carrying v3 `sec/results.tex` Table 5
caption + §6 surrounding prose, **insert** the framing sentence
near the cost-column header:

> "Pricing reflects the 2026-05-01 vendor public-rate snapshot
> (appendix), retained verbatim for reproducibility across the
> TODAES revision cycle."

Snapshot file `paper/data/pricing_2026_05_01.yaml` carries verbatim
to `v4/.../pricing_2026_05_01.yaml` (or stays under `..\data\` via
share pattern). Do not rename the file or update the date.

## Round-19+2 update: paper/plans/ + paper/scripts/ audit

Completes Section F of `v3_asset_migration_proposal.md`:

| Asset | Status | v4 action |
|---|---|---|
| `paper/plans/mlcad_d1_d11.md` | Historical, MLCAD-specific, **abandoned** | **Do not migrate**; leave as archived reference in v3 tree. v4 D-task remapping lives in `outline_v4.md`. |
| `paper/scripts/extract_transcript_logs.py` | **Active** data-pipeline script feeding `paper/data/extracted_logs.csv` | **Share via `..\scripts\` from v4** (no copy). v4 §9 reproducibility will cite this as a core artefact for the replication recipe. |

**Path implication:** `v4/main.tex` already shares `..\refs.bib` and
`..\figs\`; extending the share pattern to `..\scripts\` and
`..\data\` keeps v3 + v4 reading the same pipeline outputs.

## Phase 1 execution checklist (when leader greenlights)

1. **Tables 1-6 carry** (per `v3_asset_migration_proposal.md` §A) —
   apply Q-mig-1 revert during carry.
2. **§5.1 / §5.5 restore** from `sec5_results.md` (Q-mig-2).
3. **§2-§8 prose backbone carry** (per §C of proposal) — straight
   port; depth expansion is Phase 2, not Phase 1.
4. **Pricing-snapshot framing sentence** inserted in §6 cost column
   intro (Q-mig-5).
5. **Md-archive banners** added to all `paper/sec*.md` files
   (Q-mig-3).
6. **`analogcoderpro` decision** pending re-ruling (Q-mig-4).
7. **Build verification** — full `pdflatex → bibtex → pdflatex → pdflatex` cycle clean on v4.

## What this file is NOT

- Not a Phase 1 execution log (will be a separate file once Phase 1
  starts).
- Not a v4 outline (lives in `outline_v4.md`).
- Not a status report — purely a durable decisions log so the
  rulings survive context resets / agent handoffs.
