# Claude Code Compatibility

This file exists only because Claude Code auto-loads `CLAUDE.md`.
Canonical project instructions are vendor-neutral and live in `AGENTS.md` and
`docs/`.

Do not add project rules here. Update `AGENTS.md` or the relevant file under
`docs/` first, then keep this file as a thin compatibility pointer.

@AGENTS.md
